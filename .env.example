# Example environment variables for launch
# Fill these in for local development or deployment

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL= # Public URL for Supabase project (frontend)
NEXT_PUBLIC_SUPABASE_ANON_KEY= # Public anon key for Supabase (frontend)
SUPABASE_URL= # Backend Supabase URL (if different from public)
SUPABASE_ANON_KEY= # Backend anon key (if needed for server-side)

# --- OpenAI ---
OPENAI_API_KEY= # OpenAI API key for AI features

NEXT_PUBLIC_SITE_URL= # Public site URL (for Vercel, etc)

# Add only variables actually used by the codebase for launch
